import React from 'react'
import classes from '../style/Button.module.css'

const Button = () => {
    return (
        <div>
            <button className={classes.button}>一歩ずつ。</button>
        </div>
    )
}

export default Button
